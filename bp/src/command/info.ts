import type { Command } from "../main";
export default {
    name: "info",
    description: "Get information about the Matrix AntiCheat.",
    requireOp: false,
    execute: (args, player) => {
        
    }
} as Command;